---
title: "Alfaro Lab - Publications"
layout: gridlay
excerpt: "Alfaro Lab -- Publications."
sitemap: false
permalink: /publications-enhanced/
---

# Publications

<div class="row">
  <div class="col-md-8">
    <p>My publications are also listed on <a href="https://scholar.google.com/citations?user=_wlHRTUAAAAJ&hl=en" target="_blank">Google Scholar</a> and <a href="https://orcid.org/0000-0002-8898-8230" target="_blank">ORCID</a>.</p>
  </div>
  <div class="col-md-4">
    <div class="results-count text-right" style="margin-bottom: 15px;"></div>
  </div>
</div>

<!-- Publication Filters -->
<div class="publication-filters" style="margin-bottom: 30px; padding: 20px; background-color: var(--background-light); border-radius: 8px;">
  <h4 style="margin-bottom: 20px;">Filter Publications</h4>
  <!-- Filters will be dynamically generated by JavaScript -->
</div>

## Recent Publications

<div class="recent-publications">
{% bibliography --max 10 %}
</div>

## All Publications

<div class="all-publications">
{% assign bibliography_entries = site.bibliography %}
{% for entry in bibliography_entries %}
  <div class="publication-item" 
       data-year="{{ entry.year }}" 
       data-type="{{ entry.type | default: 'article' }}"
       data-keywords="{{ entry.keywords | join: ' ' }}">
    
    <div class="pub-title">
      {% if entry.url %}
        <a href="{{ entry.url }}" target="_blank">{{ entry.title }}</a>
      {% elsif entry.doi %}
        <a href="https://doi.org/{{ entry.doi }}" target="_blank">{{ entry.title }}</a>
      {% else %}
        {{ entry.title }}
      {% endif %}
    </div>
    
    <div class="pub-authors">
      {{ entry.author }}
    </div>
    
    <div class="pub-venue">
      {% if entry.journal %}
        <em>{{ entry.journal }}</em>
        {% if entry.volume %}, {{ entry.volume }}{% endif %}{% if entry.pages %}:{{ entry.pages }}{% endif %}
      {% elsif entry.booktitle %}
        <em>{{ entry.booktitle }}</em>
      {% endif %}
    </div>
    
    <div class="pub-year">
      {{ entry.year }}
    </div>
    
    {% if entry.abstract %}
    <div class="pub-abstract" style="display: none; margin-top: 10px; padding: 10px; background: #f8f9fa; border-radius: 4px;">
      {{ entry.abstract }}
    </div>
    <button class="btn btn-sm btn-link toggle-abstract" onclick="toggleAbstract(this)">Show Abstract</button>
    {% endif %}
    
    <div class="pub-links" style="margin-top: 10px;">
      {% if entry.doi %}
        <a href="https://doi.org/{{ entry.doi }}" target="_blank" class="btn btn-xs btn-primary">DOI</a>
      {% endif %}
      {% if entry.pdf %}
        <a href="{{ entry.pdf }}" target="_blank" class="btn btn-xs btn-success">PDF</a>
      {% endif %}
      {% if entry.url %}
        <a href="{{ entry.url }}" target="_blank" class="btn btn-xs btn-info">Link</a>
      {% endif %}
      <!-- Altmetric badge would go here -->
      {% if entry.doi %}
        <div class="altmetric-embed" data-doi="{{ entry.doi }}" data-badge-type="donut" data-badge-popover="right" style="display: inline-block; margin-left: 10px;"></div>
      {% endif %}
    </div>
  </div>
{% endfor %}

{% bibliography %}
</div>

<!-- Load required JavaScript libraries -->
<script src="https://unpkg.com/lunr/lunr.js"></script>
<script src="{{ site.baseurl }}/assets/js/interactive-features.js"></script>
<script src="{{ site.baseurl }}/assets/js/search.js"></script>

<!-- Altmetric badges -->
<script type='text/javascript' src='https://d1bxh8uas1mnw7.cloudfront.net/assets/embed.js'></script>

<script>
function toggleAbstract(button) {
  const abstract = button.previousElementSibling;
  if (abstract.style.display === 'none') {
    abstract.style.display = 'block';
    button.textContent = 'Hide Abstract';
  } else {
    abstract.style.display = 'none';
    button.textContent = 'Show Abstract';
  }
}

// Initialize publication filters when page loads
document.addEventListener('DOMContentLoaded', function() {
  // Add publication count
  updateResultsCount();
});
</script>

<style>
/* Additional styles for enhanced publications page */
.publication-item {
  padding: 1.5rem;
  border-bottom: 1px solid var(--border-light);
  transition: background-color 0.3s ease;
}

.publication-item:hover {
  background-color: var(--background-light);
}

.filter-group {
  margin-bottom: 15px;
}

.filter-group h5 {
  margin-bottom: 8px;
  color: var(--primary-blue);
}

.btn-group .btn {
  margin-right: 5px;
  margin-bottom: 5px;
}

.search-suggestions {
  position: absolute;
  background: white;
  border: 1px solid var(--border-light);
  border-radius: 4px;
  max-height: 200px;
  overflow-y: auto;
  z-index: 1000;
  width: 100%;
  display: none;
}

.suggestion-item {
  padding: 8px 12px;
  cursor: pointer;
  border-bottom: 1px solid #eee;
}

.suggestion-item:hover {
  background-color: var(--background-light);
}

.suggestion-item:last-child {
  border-bottom: none;
}

@media (max-width: 768px) {
  .publication-filters {
    padding: 15px;
  }
  
  .filter-group {
    margin-bottom: 20px;
  }
  
  .btn-group {
    display: block;
  }
  
  .btn-group .btn {
    display: block;
    width: 100%;
    margin-bottom: 5px;
    margin-right: 0;
  }
}
</style>